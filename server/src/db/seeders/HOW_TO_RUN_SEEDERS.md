# 🌱 SEEDERS - ИНСТРУКЦИЯ ЗАПУСКА

## 📋 SEEDERS (ТЕСТОВЫЕ ДАННЫЕ)

Я создал **6 seeders** с тестовыми данными для всех таблиц:

```
✅ 20250101000001-admin-seeder.js          → AdminUsers (админы)
✅ 20250101000002-animals-seeder.js        → Animals (животные)
✅ 20250101000003-photo-seeder.js          → PhotoOfAnimals (фото)
✅ 20250101000004-info-about-animals-seeder.js → InfoAboutAnimals (информация)
✅ 20250101000005-main-page-seeder.js      → Main_Page (главная страница)
✅ 20250101000006-tariffs-seeder.js        → Tariffs (тарифы)
```

---

## 🚀 ШАГИ ЗАПУСКА:

### ШАГ 1: Скопируй все seeders

Из `/mnt/user-data/outputs/` скопируй все 6 файлов seeders в папку:

```
server/src/db/seeders/
```

---

### ШАГ 2: Запусти seeders

В папке `server/` выполни:

```bash
# Запустить ВСЕ seeders
npx sequelize-cli db:seed:all
```

---

### ШАГ 3: Проверь результат

Подключись к БД и посмотри данные:

```bash
# Подключиться к БД
psql -U postgres -d zoo_db

# Посмотреть администраторов
SELECT * FROM "AdminUsers";

# Посмотреть животных
SELECT * FROM "Animals";

# Посмотреть фото
SELECT * FROM "PhotoOfAnimals";

# Посмотреть информацию о животных
SELECT * FROM "InfoAboutAnimals";

# Посмотреть главную страницу
SELECT * FROM "Main_Page";

# Посмотреть тарифы
SELECT * FROM "Tariffs";

# Выход
\q
```

---

## 📊 ТЕСТОВЫЕ ДАННЫЕ:

### 👤 Администраторы

```
email: admin@zoo.com
пароль: password123

email: staff@zoo.com
пароль: password456
```

### 🦁 Животные (8 штук)

1. **Лев** - 3 фото
2. **Слон** - 3 фото
3. **Жираф** - 2 фото
4. **Тигр** - 3 фото
5. **Обезьяна** - 2 фото
6. **Панда** - 3 фото
7. **Полярный медведь** - 2 фото
8. **Зебра** - 3 фото

### 📸 Фотографии

Всего **22 фотографии** с правильным порядком в галерее

### 📝 Информация о животных

Для каждого животного:
- Дополнительное описание
- Интересные факты (5+ фактов для каждого)

### 🏠 Главная страница

- **info**: Подробная информация о зоопарке
- **contacts**: Телефон, почта, адрес, время работы, соцсети

### 💰 Тарифы

- **tariff_weekdays**: 500 рублей (будни)
- **tariff_weekend**: 750 рублей (выходные)
- **benefits**: Информация о скидках
- **conditions**: Условия посещения

---

## 🔧 ДОПОЛНИТЕЛЬНЫЕ КОМАНДЫ:

```bash
# Откатить последний seeder
npx sequelize-cli db:seed:undo

# Откатить ВСЕ seeders
npx sequelize-cli db:seed:undo:all

# Откатить конкретный seeder
npx sequelize-cli db:seed:undo --seed 20250101000001-admin-seeder.js

# Запустить конкретный seeder
npx sequelize-cli db:seed --seed 20250101000001-admin-seeder.js
```

---

## ✅ ПРОВЕРКА:

После запуска seeders, проверь что:

```bash
# Проверь что админы созданы
psql -U postgres -d zoo_db -c "SELECT COUNT(*) FROM \"AdminUsers\";"
# Результат: 2

# Проверь что животные созданы
psql -U postgres -d zoo_db -c "SELECT COUNT(*) FROM \"Animals\";"
# Результат: 8

# Проверь что фото созданы
psql -U postgres -d zoo_db -c "SELECT COUNT(*) FROM \"PhotoOfAnimals\";"
# Результат: 22

# Проверь что информация создана
psql -U postgres -d zoo_db -c "SELECT COUNT(*) FROM \"InfoAboutAnimals\";"
# Результат: 8

# Проверь главную страницу
psql -U postgres -d zoo_db -c "SELECT * FROM \"Main_Page\";"

# Проверь тарифы
psql -U postgres -d zoo_db -c "SELECT * FROM \"Tariffs\";"
```

---

## 🎯 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК:

### Ошибка: "foreign key constraint violation"
```
Это означает, что seeder для Tariffs или MainPage запустился 
ДО seeder для AdminUsers

Решение:
1. Откатить все seeders: npx sequelize-cli db:seed:undo:all
2. Запустить заново: npx sequelize-cli db:seed:all
```

### Ошибка: "duplicate key value violates unique constraint"
```
Это означает, что seeder уже запустился и данные есть в БД

Решение:
npx sequelize-cli db:seed:undo:all
npx sequelize-cli db:seed:all
```

### БД пуста после запуска seeders
```
Это может быть если миграции не запустились правильно

Проверь статус миграций:
npx sequelize-cli db:migrate:status

Запусти миграции если нужно:
npx sequelize-cli db:migrate
```

---

## 📍 ПОРЯДОК ЗАПУСКА SEEDERS:

```
1️⃣ admin-seeder.js             (создаёт админов)
2️⃣ animals-seeder.js           (создаёт животных)
3️⃣ photo-seeder.js             (создаёт фото - зависит от животных)
4️⃣ info-about-animals-seeder.js (создаёт информацию - зависит от животных)
5️⃣ main-page-seeder.js         (зависит от админов)
6️⃣ tariffs-seeder.js           (зависит от админов)
```

✅ Seeders запускаются в правильном порядке автоматически!

---

## 🚀 БЫСТРЫЙ СТАРТ:

```bash
cd server

# Если первый раз:
npm install
createdb zoo_db
npx sequelize-cli db:migrate
npx sequelize-cli db:seed:all

# Готово! БД с данными создана!
```

---

## ✨ ДАЛЬНЕЙШИЕ ШАГИ:

Когда seeders запустятся успешно, я создам:

1. ✅ **Controllers** - обработка запросов (CRUD операции)
2. ✅ **Services** - бизнес-логика
3. ✅ **Routes** - API endpoints
4. ✅ **Middleware** - проверки и валидация
5. ✅ **Utils** - вспомогательные функции

---

**Напиши:** "Запустил seeders!" когда будет готово! 🌱
